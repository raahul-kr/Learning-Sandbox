### All DOM selectors NodeList and HTMLCollection 
### These all are happening and performing in website console:

type : document.getElementById
       eg -> document.getElementById('title')

    // i can use dot(.) to access other-other element of it.
       eg -> document.getElementById('title').id
          -> document.getElementById('title').getAttribute('class')
          -> title.style.background = 'green'
          -> title.style.padding = "15px"
          -> title.style.borderRadius = "5px"

type : These three are same but have lots of difference
        title.innerText, title.textContent, title.innerHTML

        title.innerText -> only the thing is visible on webpage is displayed
        tittle.textContent -> gives you those value which are hidden on webpages or hidden by some css property
        title.innerHTML -> gives the whole html value

###  Good Query Selector

type: document.querySelector('h2')
      document.querySelector('#title')
      document.querySelector('.heading')
      document.querySelector('input[type="password"]')  // can able to use all the CSS selector
         
      //-----------------------------------------   
      document.querySelector('ul') // can even modified it
      eg -> const myul = document.querySelector('ul')
            myul.querySelector('li')
            const turnGreen = myul.querySelector('li')
            turnGreen.style.backgroundColor = "green"
            turnGreen.style.padding = "10px"

note: learn about document.querySelectorAll

